server {
    listen 80;
    listen [::]:80;
    server_name yotradellc.com www.yotradellc.com;

    # For SSL certificate verification
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        # Redirect all HTTP requests to HTTPS
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name yotradellc.com www.yotradellc.com;

    # SSL Certificates - VALID AFTER CERTBOT RUN
    # ssl_certificate /etc/letsencrypt/live/yotradellc.com/fullchain.pem;
    # ssl_certificate_key /etc/letsencrypt/live/yotradellc.com/privkey.pem;

    # SSL Parameters
    # include /etc/letsencrypt/options-ssl-nginx.conf;
    # ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        proxy_pass http://app:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        
        # Real IP Forwarding
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
